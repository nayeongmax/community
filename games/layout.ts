// 게임 화면의 기준 크기
//
// 모든 게임이 같은 폭을 쓰도록 한 곳에 모아 둔다.
// 이 값을 키우면 게임 전체가 함께 커진다.

/** 게임 한 판이 차지하는 가로 폭 (px) */
export const GAME_W = 440;

/** 게임 안에서 공통으로 쓰는 상단 점수줄 */
export const BAR = 'flex items-center justify-between text-base font-bold text-slate-700';
